# Triangle Art (삼각형으로 만드는 그림) PRD v1.0

## 1. 제품 정의
- **제품명**: Triangle Art / 삼각형으로 만드는 그림
- **목적**: 사용자가 삼각형만을 생성, 이동, 변형, 복제, 색칠하여 동물, 사물, 풍경 등 다채로운 그림을 그리는 창작형 게임.
- **성격**: 창작 80% : 학습 활용 20% (초등 수학 도형 단원 학습 후 자연스럽게 각도와 변을 체감하며 창작 활동 진행).
- **타깃 플랫폼**: 웹 브라우저 (PC & 태블릿) 및 데스크톱.

## 2. 핵심 기능 요구사항

### F-01. 새 삼각형 생성
- 상단 툴바의 **"새 삼각형"** 버튼 클릭 시 캔버스 중앙에 초기 **정삼각형** 생성.
- 기본 크기: 변 길이 2~3 그리드 단위(약 80~120px).
- 초기 각도: 60° / 60° / 60° (내각의 합 180°).
- 생성된 삼각형은 자동으로 선택 상태가 됨.

### F-02. 삼각형 이동 (Body Drag)
- 삼각형 내부 클릭 & 드래그로 캔버스 상에서 자유롭게 이동.
- 이동 중 삼각형의 형태(세 변의 길이, 세 각도, 크기)는 엄격하게 유지.
- 스냅 모드 활성화 시 이동 후 가까운 그리드에 스냅.

### F-03. 꼭짓점 편집 (Vertex Drag)
- 선택된 삼각형의 세 꼭짓점에 원형 조절 핸들 표시.
- 핸들을 드래그하여 꼭짓점을 이동시키며 삼각형을 자유롭게 변형.
- 최소 유효 면적 보장 및 세 점이 일직선이 되는 퇴화 삼각형 방지.
- 스냅 모드 활성화 시 꼭짓점이 그리드 교차점에 부드럽게 흡착.

### F-04. 실시간 수치 및 각도 표시
- 각 꼭짓점 근처에 해당 내각(예: 60°, 90°, 45°)을 정수 단위(°)로 표시.
- 세 각도의 합은 항상 180°로 유지되도록 반올림 오차 보정.
- 각 변의 중앙에 해당 변의 길이(그리드 1칸 = 1단위 기준)를 소수점 1자리로 표시.

### F-05. 삼각형 분류 자동 판별
- 선택된 삼각형의 기하학적 특성을 실시간 판별하여 UI 배지에 표시:
  - **정삼각형**: 세 변의 길이가 같고 세 각이 모두 60°
  - **직각삼각형**: 한 각이 90°
  - **이등변삼각형**: 두 변의 길이가 같음
  - **예각삼각형**: 세 각이 모두 90° 미만
  - **둔각삼각형**: 한 각이 90° 초과

### F-06. 복제 (Duplicate)
- 선택된 삼각형과 동일한 형태, 각도, 변 길이, 색상을 가진 새 삼각형 생성.
- 원본과 겹치지 않도록 우하단(+20px, +20px) 오프셋 배치 후 자동 선택.

### F-07. 색상 팔레트
- 12색 어린이 친화형 직관적 팔레트 (빨, 주, 노, 연두, 초록, 하늘, 파랑, 보라, 분홍, 갈색, 회색, 검정).
- 색상 클릭 시 현재 선택된 삼각형의 면 색상이 즉시 변경됨.

### F-08. 되돌리기 / 다시 실행 (Undo / Redo)
- 생성, 삭제, 이동, 꼭짓점 변형, 색상 변경, 복제 등 모든 작업에 대해 Undo/Redo 지원.
- 단축키: `Ctrl + Z` (Undo), `Ctrl + Y` (Redo).

### F-09. 그리드 및 스냅 시스템
- 캔버스 전체에 얇고 선명한 모눈 격자 렌더링 (1칸 = 1단위 = 40px).
- **스냅 ON/OFF** 토글 버튼 제공.

### F-10. 순수 작품 PNG 내보내기 (Export PNG)
- **PNG 저장** 버튼 클릭 시 완성된 작품을 이미지로 저장.
- 저장 결과물에는 그리드 격자, 조절 핸들, 각도/변길이 라벨, 선택 테두리, UI 요소가 일절 포함되지 않고 순수 삼각형 그림만 렌더링.
- 배경 옵션: 투명 배경 / 흰색 배경 토글.
